# Dependencies for the FastAPI form builder
fastapi>=0.110.0
python-multipart>=0.0.9
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
email-validator>=2.1.0.post1
dnspython>=2.6.1
# ip2geotools kept as fallback; remove if you fully deprecate it
ip2geotools>=0.1.6
firebase-admin>=6.5.0
Jinja2>=3.1.4
bleach>=6.1.0
# Disposable email domains list (5000+)
disposable-email-domains>=0.0.89

# Signature verification per Standard Webhooks
standardwebhooks>=0.4.1

# Email provider SDK (Resend)
resend>=2.0.0
slowapi>=0.1.8
# IP-based signup throttling (server-side)
fastratelimiter>=0.0.4
itsdangerous>=2.2.0
requests>=2.31.0
rapidfuzz>=3.6.1
python-whois>=0.8.0
boto3>=1.34.0
# Image MIME verification and safe re-encode
python-magic>=0.4.27
Pillow>=10.3.0
# Translation providers wrapper
deep-translator>=1.11.4
# For encrypting tokens and SMTP credentials (Fernet)
cryptography>=41.0.0
# URL parsing and validation for malicious link scanning
tldextract>=5.1.2
validators>=0.22.0
# Google Safe Browsing client (using stdlib HTTP, no extra lib needed)
# Ensure public suffix list updates for tldextract in production
publicsuffixlist>=0.10.0
pyshorteners>=1.0.1
# ClamAV virus scanning client
clamd>=1.0.2
